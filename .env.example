# Copiar a .env (ignorado por git) y completar con los valores reales.
# Dashboard de Supabase → tu proyecto → Settings → API.

SUPABASE_URL=https://TU-PROYECTO.supabase.co
SUPABASE_KEY=tu-anon-public-key
